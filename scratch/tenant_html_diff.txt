diff --git a/src/main/resources/static/tenant/index.html b/src/main/resources/static/tenant/index.html index 3d0034a..fef4b5c 100644 --- a/src/main/resources/static/tenant/index.html +++ b/src/main/resources/static/tenant/index.html @@ -930,6 +930,12 @@                      <span>Billing & Subscriptions</span>                  </a>              </li> +            <li class="sidebar-item" data-tab="tab-api-logs"> +                <a class="sidebar-link"> +                    <i class="fa-solid fa-terminal"></i> +                    <span>API Logs</span> +                </a> +            </li>          </ul>            <div class="sidebar-footer"> @@ -1211,6 +1217,64 @@                  </div>              </section>   +            <!-- ========================================== +                 TAB: API HIT LOGS +                 ========================================== --> +            <section id="tab-api-logs" class="view-panel"> +                <div class="card" style="margin-bottom: 24px;"> +                    <div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 16px;"> +                        <div> +                            <h3 class="card-title"><i class="fa-solid fa-terminal" style="color: var(--accent-violet);"></i> Tenant API Hit Logging</h3> +                            <p class="card-subtitle" style="margin-top: 4px;">Monitor API hit requests, client IPs, and device details for your organization</p> +                        </div> +                        <div style="display: flex; align-items: center; gap: 12px;"> +                            <label class="form-label" for="tenant-api-log-toggle" style="margin: 0; font-weight: 600;">Enable Log Visibility:</label> +                            <input type="checkbox" id="tenant-api-log-toggle" style="width: 22px; height: 22px; accent-color: var(--accent-violet); cursor: pointer;" onchange="handleTenantApiLogToggle(this.checked)"> +                        </div> +                    </div> +                </div> + +                <div class="card" id="tenant-api-logs-card"> +                    <div class="filter-bar"> +                        <div class="search-input-wrapper" style="opacity: 0.7;"> +                            <i class="fa-solid fa-info-circle"></i> +                            <span style="margin-left: 20px; font-size: 0.85rem; color: var(--text-secondary);">Real-time asynchronous API log events</span> +                        </div> +                        <button class="btn btn-secondary btn-sm" onclick="fetchTenantApiLogs()"> +                            <i class="fa-solid fa-rotate"></i> Refresh Logs +                        </button> +                    </div> + +                    <div class="table-container"> +                        <table class="responsive-table"> +                            <thead> +                            <tr> +                                <th>Time</th> +                                <th>Method</th> +                                <th>URI</th> +                                <th>IP Address</th> +                                <th>Device Name</th> +                                <th>User Email</th> +                            </tr> +                            </thead> +                            <tbody id="tenant-api-logs-tbody"> +                            <tr> +                                <td colspan="6" style="text-align: center; color: var(--text-muted); padding: 20px;"> +                                    Click Refresh to load API logs. +                                </td> +                            </tr> +                            </tbody> +                        </table> +                    </div> +                     +                    <div style="display: flex; justify-content: space-between; align-items: center; margin-top: 20px; padding-top: 20px; border-top: 1px solid var(--border-color);"> +                        <button class="btn btn-secondary btn-sm" id="btn-tenant-api-logs-prev" onclick="changeTenantApiLogsPage(-1)">Previous</button> +                        <span id="tenant-api-logs-page-info" style="font-size: 0.9rem; color: var(--text-secondary);">Page 1 of 1</span> +                        <button class="btn btn-secondary btn-sm" id="btn-tenant-api-logs-next" onclick="changeTenantApiLogsPage(1)">Next</button> +                    </div> +                </div> +            </section> +              <!-- ==========================================                   TAB 5: RUN PAYRUN                   ========================================== --> @@ -2235,7 +2299,8 @@          'tab-payruns': 'Run Payrun',          'tab-my-salary': 'My Salary & History',          'tab-config': 'Payroll Config', -        'tab-billing': 'Billing & Subscriptions' +        'tab-billing': 'Billing & Subscriptions', +        'tab-api-logs': 'API Hit Logs'        };        document.getElementById('current-tab-title').textContent = titles[tabId] || 'Dashboard';   @@ -2254,6 +2319,7 @@        else if (tabId === 'tab-my-salary') fetchMySalaryHistory(loggedInEmployeeId);        else if (tabId === 'tab-config') fetchGlobalConfig();        else if (tabId === 'tab-billing') fetchBillingData(); +      else if (tabId === 'tab-api-logs') fetchTenantApiLogs(0);      }        // ========================================== @@ -4133,6 +4199,104 @@      window.loadCalendarFromSelectors = loadCalendarFromSelectors;      window.closeModal = closeModal;   +    // ========================================== +    // API HIT LOGGING +    // ========================================== +    let tenantApiLogsPage = 0; +    let tenantApiLogsTotalPages = 1; + +    async function fetchTenantApiLogs(page = 0) { +      tenantApiLogsPage = page; +      const tbody = document.getElementById('tenant-api-logs-tbody'); +      tbody.innerHTML = '<tr><td colspan="6" style="text-align: center;"><span class="loading-spinner"></span> Loading API logs...</td></tr>'; + +      try { +        const res = await fetch(`${API_BASE}/api-logs?page=${page}&size=10&sort=hitTime,desc`, { +          headers: getHeaders() +        }); + +        if (res.ok) { +          document.getElementById('tenant-api-log-toggle').checked = true; +           +          const data = await res.json(); +          tbody.innerHTML = ''; +          tenantApiLogsTotalPages = data.totalPages || 1; +          document.getElementById('tenant-api-logs-page-info').textContent = `Page ${tenantApiLogsPage + 1} of ${tenantApiLogsTotalPages}`; + +          document.getElementById('btn-tenant-api-logs-prev').disabled = tenantApiLogsPage <= 0; +          document.getElementById('btn-tenant-api-logs-next').disabled = tenantApiLogsPage >= tenantApiLogsTotalPages - 1; + +          const logs = data.content || []; +          if (logs.length > 0) { +            logs.forEach(log => { +              const row = document.createElement('tr'); +              const timeStr = log.hitTime ? new Date(log.hitTime).toLocaleString() : '-'; +              const methodBadge = log.httpMethod === 'GET' ? 'badge-info' : +                                  log.httpMethod === 'POST' ? 'badge-success' : +                                  log.httpMethod === 'DELETE' ? 'badge-danger' : 'badge-warning'; +               +              row.innerHTML = ` +                <td>${timeStr}</td> +                <td><span class="badge ${methodBadge}">${log.httpMethod}</span></td> +                <td><code style="background: rgba(255,255,255,0.05); padding: 2px 6px; border-radius: 4px; font-family: monospace;">${log.requestUri}</code></td> +                <td>${log.ipAddress || '-'}</td> +                <td><i class="fa-solid fa-laptop-code" style="color: var(--accent-violet); margin-right: 6px;"></i> ${log.deviceName || 'Unknown'}</td> +                <td><strong>${log.employeeEmail || 'anonymous'}</strong></td> +              `; +              tbody.appendChild(row); +            }); +          } else { +            tbody.innerHTML = '<tr><td colspan="6" style="text-align: center; color: var(--text-muted);">No API logs found.</td></tr>'; +          } +        } else { +          document.getElementById('tenant-api-log-toggle').checked = false; +          tbody.innerHTML = ` +            <tr> +              <td colspan="6" style="text-align: center; color: var(--text-secondary); padding: 30px;"> +                <i class="fa-solid fa-lock" style="font-size: 2rem; color: var(--text-muted); display: block; margin-bottom: 12px;"></i> +                API hit logging details are currently hidden/disabled for this tenant.<br> +                <span style="font-size: 0.85rem; color: var(--text-muted);">Check "Enable Log Visibility" above to display them.</span> +              </td> +            </tr> +          `; +          document.getElementById('btn-tenant-api-logs-prev').disabled = true; +          document.getElementById('btn-tenant-api-logs-next').disabled = true; +          document.getElementById('tenant-api-logs-page-info').textContent = 'Page 1 of 1'; +        } +      } catch (err) { +        tbody.innerHTML = '<tr><td colspan="6" style="text-align: center; color: var(--accent-rose);">Error connecting to API log service.</td></tr>'; +      } +    } + +    async function handleTenantApiLogToggle(checked) { +      try { +        const res = await fetch(`${API_BASE}/api-logs/toggle?enabled=${checked}`, { +          method: 'PUT', +          headers: getHeaders() +        }); +        if (res.ok) { +          showToast('Success', `API log visibility has been ${checked ? 'enabled' : 'disabled'}.`, 'success'); +          fetchTenantApiLogs(0); +        } else { +          const err = await res.json(); +          showToast('Toggle Failed', err.message || 'Could not update setting.', 'error'); +        } +      } catch (err) { +        showToast('Error', 'Connection error while updating logging setting.', 'error'); +      } +    } + +    function changeTenantApiLogsPage(direction) { +      const targetPage = tenantApiLogsPage + direction; +      if (targetPage >= 0 && targetPage < tenantApiLogsTotalPages) { +        fetchTenantApiLogs(targetPage); +      } +    } + +    window.fetchTenantApiLogs = fetchTenantApiLogs; +    window.handleTenantApiLogToggle = handleTenantApiLogToggle; +    window.changeTenantApiLogsPage = changeTenantApiLogsPage; +      // ==========================================      // INIT      // ==========================================